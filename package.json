{
  "name": "interviews-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/**/*",
    "packages/**/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "packageManager": "pnpm@10.7.1",
  "devDependencies": {
    "turbo": "2.5.0",
    "typescript": "5.8.3"
  }
}
